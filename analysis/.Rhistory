library(bruceR)
disdata = read.csv('disdata.csv')
setwd("C:/Users/scy/Desktop/GPTexperiment1D/analysis")
reward_data = read.csv('reward_data.csv')
knitr::opts_chunk$set(echo = TRUE)
library(bruceR)
disdata = read.csv('disdata.csv')
MANOVA(disdata,dv='Edistance',between=c('agent','environment'),within='horizons',subID='id')
reward_data = read.csv('reward_data.csv')
MANOVA(reward_data,dv='z',between=c('agent','environment'),subID='id')
View(reward_data)
reward_data = read.csv('reward_data.csv')
MANOVA(reward_data,dv='z',between=c('agent','environment'),subID='id')
View(reward_data)
View(reward_data)
reward_data = read.csv('reward_data.csv')
MANOVA(reward_data,dv='z',between=c('agent','environment'),subID='id')
per_rewards = read.csv('per_rewards.csv)
per_rewards = read.csv('per_rewards.csv')
per_rewards = read.csv('per_rewards.csv')
smooth_reward = per_rewards[per_rewards$environment=='smooth']
View(per_rewards)
smooth_reward = subset(per_rewards,environment=='smooth')
View(smooth_reward)
t.test(smooth_reward$z,mu=50)
t.test(rough_reward$z,mu=50)
```{r}
per_rewards = read.csv('per_rewards.csv')
smooth_rewards = subset(per_rewards,environment=='smooth')
t.test(smooth_rewards$z,mu=50)
rough_reward = subset(per_rewards,environment=='rough')
t.test(rough_reward$z,mu=50)
rough_reward = subset(per_rewards,environment=='rough')
t.test(rough_reward$z,mu=50)
per_rewards = read.csv('per_rewards.csv')
smooth_rewards = subset(per_rewards,environment=='smooth')
t.test(smooth_rewards$z,mu=50)
rough_reward = subset(per_rewards,environment=='rough')
t.test(rough_reward$z,mu=50)
rough_reward = subset(per_rewards,environment=='rough')
t.test(rough_reward$z,mu=50)
per_rewards = read.csv('per_rewards.csv')
smooth_rewards = subset(per_rewards,environment=='smooth')
t.test(smooth_rewards$z,mu=50)
rough_reward = subset(per_rewards,environment=='rough')
t.test(rough_reward$z,mu=50)
per_rewards = read.csv('per_rewards.csv')
smooth_rewards = subset(per_rewards,environment=='smooth')
t.test(smooth_rewards$z,mu=50)
rough_reward = subset(per_rewards,environment=='rough')
t.test(rough_reward$z,mu=50)
library(bruceR)
disdata = read.csv('disdata.csv')
MANOVA(disdata,dv='Edistance',between=c('agent','environment'),within='horizons',subID='id')
reward_data = read.csv('reward_data.csv')
MANOVA(reward_data,dv='z',between=c('agent','environment'),subID='id')
per_rewards = read.csv('per_rewards.csv')
smooth_rewards = subset(per_rewards,environment=='smooth')
t.test(smooth_rewards$z,mu=50)
rough_reward = subset(per_rewards,environment=='rough')
t.test(rough_reward$z,mu=50)
per_rewards = read.csv('per_rewards.csv')
smooth_rewards = subset(per_rewards,environment=='smooth')
t.test(smooth_rewards$z,mu=50)
rough_reward = subset(per_rewards,environment=='rough')
t.test(rough_reward$z,mu=50)
View(rough_reward)
reward_data = read.csv('reward_data.csv')
MANOVA(reward_data,dv='z',between=c('agent','environment'),subID='id')
per_rewards = read.csv('per_rewards.csv')
smooth_rewards = subset(per_rewards,environment=='smooth')
t.test(smooth_rewards$z,mu=50)
rough_reward = subset(per_rewards,environment=='rough')
t.test(rough_reward$z,mu=50)
per_rewards = read.csv('per_rewards.csv')
smooth_rewards = subset(per_rewards,environment=='smooth')
t.test(smooth_rewards$z,mu=50)
rough_reward = subset(per_rewards,environment=='rough')
t.test(rough_reward$z,mu=50)
View(reward_data)
per_rewards = read.csv('per_rewards.csv')
smooth_rewards = subset(per_rewards,environment=='smooth')
t.test(smooth_rewards$z,mu=50)
rough_reward = subset(per_rewards,environment=='rough')
t.test(rough_reward$z,mu=50)
per_rewards = read.csv('per_rewards.csv')
smooth_rewards = subset(per_rewards,environment=='smooth')
t.test(smooth_rewards$z,mu=50)
rough_reward = subset(per_rewards,environment=='rough')
t.test(rough_reward$z,mu=50)
per_rewards = read.csv('per_rewards.csv')
smooth_rewards = subset(per_rewards,environment=='smooth')
t.test(smooth_rewards$z,mu=50)
rough_reward = subset(per_rewards,environment=='rough')
t.test(rough_reward$z,mu=50)
per_rewards = read.csv('per_rewards.csv')
smooth_rewards = subset(per_rewards,environment=='smooth')
t.test(smooth_rewards$z,mu=50)
rough_reward = subset(per_rewards,environment=='rough')
t.test(rough_reward$z,mu=50)
